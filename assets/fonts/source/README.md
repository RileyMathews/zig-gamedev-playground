# UI Font

`ui.ttf` is the source font used by `just fonts` to generate the committed MSDF atlas artifacts in `assets/fonts/generated/`.

The initial file is Noto Sans Regular, which is distributed under the SIL Open Font License. See `LICENSE-NotoSans.txt`.
